{
  "name": "@wotta/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "starknet": "10.5.0"
  },
  "devDependencies": {
    "@types/node": "24.0.15",
    "typescript": "5.8.3"
  },
  "scripts": {
    "build": "scarb build",
    "test": "snforge test && node --experimental-strip-types --test scripts/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
