import { assertStarknetRpcNetwork, loadConfig } from "./config.ts"; import { createDb } from "./db/client.ts"; import { createLogger, safeError } from "./logger.ts"; import { runRelayerLoop } from "./relayer/run.ts";
const config = loadConfig(); const controller = new AbortController(); process.once("SIGINT", () => controller.abort()); process.once("SIGTERM", () => controller.abort()); assertStarknetRpcNetwork(config).then(() => runRelayerLoop({ config, db: createDb(config), log: createLogger(config) }, controller.signal)).catch((error) => { console.error(safeError(error)); process.exit(1); });
